import { CarTransformInterceptor } from './car-transform.interceptor';

describe('CarTransformInterceptor', () => {
  it('should be defined', () => {
    expect(new CarTransformInterceptor()).toBeDefined();
  });
});
